# AI 開發成本優化與 Agent 工作流實戰 教學課程

這是一門為軟體工程師、技術主管與開發團隊設計的 **AI 寫程式成本優化與高效協作實戰課程**。未來開發者的差距，不僅僅在於「誰比較會寫 Prompt」，更在於「誰能用最少的 Token 成本，產出最高品質的程式碼」。

本課程仿照多奇教育訓練的四層治理結構，為您深入剖析 Token 成本成因、快取省錢祕法、高效工作流設計，以及團隊監控與治理實踐。

---

## 課程大綱

```mermaid
graph TD
    A[AI 開發成本治理] --> B[第一層：看懂成本]
    A --> C[第二層：降低浪費]
    A --> D[第三層：設計流程]
    A --> E[第四層：團隊治理]

    B --> B1(Token 計費結構)
    B --> B2(Cache 快取原理)
    C --> C1(任務拆分與 TDD)
    C --> C2(上下文防污染)
    D --> D1(分支探索 /fork)
    D --> D2(對話壓縮 /compact)
    E --> E1(LiteLLM 監控)
    E --> E2(團隊用量治理)
```

---

## 第一單元：AI 寫程式進入用量計費時代

隨著前沿 AI 模型（如 Claude 3.5 Sonnet, GPT-4o 等）在開發工具（如 Cursor, Claude Code, Antigravity）中扮演越來越重的主動代理人（Agent）角色，我們迎來了程式開發的高效時代，卻也面臨了前所未有的「Token 帳單焦慮」。

### 1. 開發者面臨的新現實
* **從「買訂閱」到「控用量」的轉變**：過去我們習慣每個月支付固定訂閱費（如 GitHub Copilot 的 $19/月）無限使用。然而隨著開發工具內建「前沿模型」倍率的調整，高強度使用或跑 Agent 任務時，預設額度經常不到半個月就燒光。許多工具已逐步轉向「用量計費（Pay-as-you-go）」或在用盡超額後收取高額費用。
* **零數據保留（Zero Data Retention, ZDR）的合規挑戰**：在金融、政府或合資企業等高度受管制產業中，不僅需要控管開發成本，還必須遵循嚴格的資安政策（如禁止數據流向境外、不可作為模型訓練資料）。如何在使用 API 時達成安全且高性價比的成本配置，是技術決策者的核心功課。

### 2. 為什麼 AI 寫程式比聊天更燒 Token？
在一般的對話聊天（如網頁版 ChatGPT）中，每次發言的上下文是線性的，且多為文字。
而在 AI 寫程式（Coding Agent）中，每次與 Agent 交互時，工具會自動背負大量的**隱性成本**：
1. **Repository Context (專案上下文)**：為了讓 Agent 了解程式碼，工具會利用向量搜尋（RAG）或直接把多個相關程式碼檔案（如 `game.js`, `player.js`）的全文拼接到每一次的 Input 中。
2. **工具輸出 (Tool Outputs)**：當 Agent 執行 `grep_search`、`run_command`（如跑測試）或讀取大檔案時，工具返回的冗長終端輸出、測試錯誤堆疊（Stack Trace）都會原封不動地塞進下一次的對話上下文。
3. **重試與修復成本**：當 Agent 嘗試修復 Bug 失敗而陷入「猜測-報錯-再猜測」的死循環時，對話上下文會像滾雪球一樣急劇膨脹。

---

## 第二單元：Token 成本結構與快取省錢原理

在用量計費時代，理解 Token 的消耗結構是優化開支的基礎。特別是 **Prompt Caching (提示詞快取)** 技術，它是讓對話費用降低 50%~90% 的絕對主力。

### 1. Token 消耗結構解析
* **Input Tokens (輸入)**：發送給模型的內容，包括歷史對話、提示詞、自動載入的程式碼檔案、工具執行結果。
* **Output Tokens (輸出)**：模型生成的回覆內容。**Output Token 的單價通常是 Input Token 的 3 到 4 倍**。
* **Cached Tokens (快取輸入)**：
  * **Anthropic (Claude)**：提供 `cache_control` 功能。寫入快取（Cache Write）會稍微加收費用，但讀取快取（Cache Read）的單價**僅為普通 Input Token 的 10%**。
  * **OpenAI (GPT-4o)**：自動對重複的 Prompt Prefix 進行快取，快取命中的 Token **直接享有 50% 折扣**。

### 2. Prompt Caching 的運作原理與規則
當我們向模型發送請求時，如果請求的**前綴（Prefix）**與之前某次請求完全一致，模型就可以複用之前已經處理過的快取，而不需重新計算。

```text
一次命中快取的對話流程：
[對話 1] (無快取) -> System Prompt + 檔案內容 (10,000 詞) -> 正常計費 (100% 價格)
[對話 2] (命中快取) -> System Prompt + 檔案內容 (10,000 詞) + 新問題 -> 命中快取的 10,000 詞僅收 10% 價格！
```

> [!IMPORTANT]
> **破壞快取效益的常見錯誤做法**：
> 在對話的中間插入「變動的內容」。例如：在 System Prompt 中加入動態時間、在對話中間臨時貼入一個大日誌、或是每次對話都修改最前面的全域設定。這樣會導致後半段的所有快取全部失效，被迫以 100% 價格重新計費！

### 3. 設定檔（AGENTS.md, CLAUDE.md）與快取成本
當使用 Agent 開發工具時，全域引導文件（如 `.agents/AGENTS.md`、`.claude/skills/.../SKILL.md`）會被載入為 System Prompt 的一部分。
* **為什麼它們影響長期成本？**
  * 這些設定檔通常位於 Prompt 的最前面（Prefix）。如果它們的內容保持**穩定不變**，那麼在同一個對話 Session 中，後續的每一次交互都能 100% 命中快取。
  * 如果您在開發過程中，頻繁手動修改 `AGENTS.md` 或 `CLAUDE.md`，這會導致**每一次對話的快取都被徹底擊穿**，造成 Token 開銷翻倍。
* **利用 Agent Skills 節省 Token**：
  * 將功能性指示（如「如何進行單元測試」、「中文 commit 規範」）以 **Skills**（獨立的子 Markdown 檔案，如 `SKILL.md`）來模組化，只在特定任務需要時透過 Link `@skills/xxx/SKILL.md`「懶加載」引入。這樣可以維持主 System Prompt 的簡潔，避免不必要的 Token 浪費。

---

## 第三單元：降低 Token 浪費的個人實戰技巧

在個人日常開發中，許多 Token 浪費並非模型價格問題，而是**任務交代與操作方式不當**導致的。透過以下四個關鍵技巧，您可以用極低的 Token 成本，取得相同的開發成果。

### 1. 任務拆小原則 (Task Decomposing)
* **痛點**：將一個過於龐大且模糊的需求（例如：「幫我重構戰鬥系統，並新增 5 種新技能與被動天賦」）直接丟給 Agent。
* **後果**：Agent 需要同時讀取、修改十幾個檔案，且極易在代碼整合時發生錯誤。每一次出錯、重試，都會將之前讀取的龐大上下文（數萬 Token）重複發送，造成嚴重的 Token 浪費。
* **解法**：**先拆分，再執行**。
  * **第一步**：請 Agent 先進行 `brainstorming`，只產出「設計規格書」與「實現計劃」（不修改代碼）。
  * **第二步**：將計劃拆分成數個 2~5 分鐘可以完成的小任務（如：建立資料結構 ➡️ 撰寫單元測試 ➡️ 實作邏輯 ➡️ 渲染特效）。
  * **第三步**：逐個任務推進，每完成一步就進行 Git Commit。

### 2. 不要一開始就讓 AI 掃描全專案
* **痛點**：隨意呼叫 `grep_search` 搜尋全專案，或直接使用模糊提問讓 Agent 自動掃描所有目錄。
* **後果**：當專案規模變大，一次全專案掃描會載入數十萬位元組的程式碼與 metadata，直接塞滿對話上限並產生高額的 Input Token 費用。
* **解法**：
  * **精確引路**：直接在提問中 @ 具體的檔案。例如：「請看 [playerCore.js](file:///d:/github/chiisen/survivor.js/js/playerCore.js#L15-L30) 中的 `pickupRange` 更新邏輯...」。
  * **過濾條件**：在使用搜尋工具時，善用包含條件（`Includes`）過濾掉無關目錄（如 `node_modules`、`.git`、`.temp`）。

### 3. 不要盲目貼入大日誌 (Log Filtering)
* **痛點**：測試失敗或程式崩潰時，直接把幾百行的 console 輸出或整個 log 檔案全部貼進對話。
* **後果**：日誌中 90% 是重複的堆疊訊息或無害的 warning，這會稀釋關鍵錯誤，並消耗大量 Token。
* **解法**：
  * **提取精華**：只貼出「錯誤型別 (Error Type)」、「錯誤訊息 (Message)」與「出錯的檔案與行號 (File & Line Number)」。
  * **硬斷言/軟檢查**：在寫程式時實踐 Fail-Fast，讓程式碼在出錯時直接印出高解析度、乾淨的單行 Crash 報告。

### 4. 模型分級調配策略
不同的開發任務需要不同等級的推理能力。混用模型是省錢的王道：
* **機械性任務**（如：重複性的 Unit Test 编写、補寫註解、簡單的 Getter/Setter 暴露）：使用**快速、便宜的小模型**（如 GPT-4o-mini 或 Claude 3 Haiku）。
* **邏輯整合與 Bug 排查**（如：處理多個檔案間的狀態同步、複雜的碰撞算法）：使用**標準的主力模型**（如 GPT-4o 或 Claude 3.5 Sonnet）。
* **架構設計與規格澄清**（如：實作全新的掉落道具系統、規劃系統解耦）：使用**最強的推理模型**（如 GPT-4o、Claude 3.5 Sonnet）。

---

## 第四單元：子任務分工、分支探索與上下文控管

當對話對談拉長時，上下文（Context Window）會被大量無關的嘗試、代碼修改歷史污染。學習如何進行**上下文隔離與壓縮**，是進階開發者的省錢核心指標。

### 1. 為什麼子任務分工能降低主上下文污染？
在進行複雜開發時，主對話（Host Session）應該只保留**「設計決策」與「高層級架構」**。
* **做法**：當需要實作某個具體、獨立的子元件（例如：撰寫一個 `objectPool.js` 的排程器）時，不要在主對話中要求 AI 直接寫代碼。
* **子智慧體 (Subagents)**：分派一個全新的、沒有繼承歷史對話的「子智慧體（Subagent）」，給予它明確的任務規格，並限制其只讀取特定的檔案。子智慧體在乾淨的沙箱中完成開發與測試後，只將最終的 code 帶回主對話。
* **收益**：主對話避免了數十次「修改-跑測試-出錯-再修改」產生的冗長歷史，維持主 System Prompt 快取的最高命中率。

### 2. 分支探索與上下文分流工具
開發工具提供了特殊的 Slash 指令來協助我們管理上下文：
* **`/fork` (分支對話)**：
  * **時機**：當您在實作一個新功能時，突然遇到一個不確定的技術選擇（例如：該用 Canvas Grid 還是 Quadtree 來優化碰撞效能？）。
  * **作用**：執行 `/fork` 會複製當前的對話狀態到一個全新的獨立視窗。您可以在新視窗進行發散式的探索與實驗。一旦確定做法，再將代碼手動合併回主分支，並直接關閉 fork 視窗。
  * **收益**：主對話不會被龐大的實驗代碼、錯誤嘗試所污染。
* **`/side` 與 `/btw` (旁支提問)**：
  * **時機**：在寫程式的過程中，突然想問一個無關緊要的知識性問題（例如：「JavaScript 中的 Map 效能是否比 Object 好？」）。
  * **作用**：開闢一個零脈絡的暫時會話回答此問題。不要在核心開發對話中提問，這能防止無關對話一直佔用快取空間。

### 3. 對話壓縮 (`/compact`) 與 Checkpoint 設定
當對話確實變得非常長（例如超過 3 萬 Token），快取命中已經無法挽救過長的處理時間與費用時，就需要進行對話壓縮。
* **`/compact` (對話壓縮)**：會清除對話歷史，只留下一份高度濃縮的摘要。
* **快取擊穿風險**：壓縮後，舊的快取會全部被判定為過期，下一次對話模型必須重新載入新的摘要並寫入快取。
* **Checkpoint 的重要性**：在執行 `/compact` 壓縮前，**必須建立 Checkpoint**。
  * 做法：手動或自動產出一份 `task.md` 與 `implementation_plan.md`，詳盡記錄「已完成哪些部分」、「目前卡在什麼問題」、「下一個具體步驟是什麼」以及「目前的架構決定」。
  * 唯有 Checkpoint 寫得足夠精準，壓縮後的 AI 才能無縫接軌，不致產生「記憶喪失」而重新猜測程式碼。

---

## 第五單元：MCP 成本黑洞與工具輸出控管

模型上下文協議 (Model Context Protocol, MCP) 賦予了 AI 連接資料庫、執行命令、讀取網路資源的超能力。然而，**未經設計與限流的 MCP 工具，是開發成本暴增的頭號兇手**。

### 1. 為什麼 MCP 容易成為 Token 黑洞？
當 Agent 呼叫一個 MCP 工具時（例如：查詢某個數據表結構或讀取終端日誌），如果該工具返回了「未經過濾的全文」，這些龐大的文字（往往有數萬字）會被直接注入至下一輪 Input。更糟糕的是，如果 Agent 無法順利解析該輸出，它會反覆呼叫、重試，每一次重試都會以加倍的費用計費。

```text
[黑洞示範]：
AI 執行命令 `npm run test`。
測試框架跑了 200 個測試，終端輸出了 5,000 行進度日誌。
工具將 5,000 行日誌全部丟給 AI。
AI 耗費了 40,000 Input Token 來讀取毫無價值的 "PASS test-xxx.js"。
```

### 2. 工具輸出的五個控管原則
為防止 MCP 工具燒毀您的額度，在設計或使用工具時，必須堅守以下五個限流原則：

1. **Lazy Loading (懶加載/摘要優先)**：
   * 工具應優先回傳「中繼資料（Metadata）」或「行數摘要」，而非內容全文。
   * 範例：讀取目錄時，只顯示檔案名稱與大小，等 AI 明確指定某一檔案時，再呼叫讀檔工具讀取該檔。
2. **Chunking & Paging (分塊分頁)**：
   * 檢視檔案工具（如 `view_file`）應限制單次最大讀取行數（例如最多 800 行），並要求 AI 帶上 `StartLine` 與 `EndLine`。
3. **Smart Grep (精確檢索)**：
   * 搜尋代碼時，優先使用 `grep_search` 來定位特定 Pattern，直接取得「符合條件的程式碼片段及行號」，避免使用模糊搜尋後把整份檔案拉下來看。
4. **回傳「下一步需要的資訊」，而非「所有資訊」**：
   * 在設計自訂的 MCP 伺服器時，應該在工具端完成資訊的「過濾與解析」。
   * 例如：查詢資料庫時，不要回傳 `SELECT *` 的 JSON 陣列，而是在工具內部加工為簡短的 Markdown 表格，僅保留關聯欄位。
5. **對錯誤進行限流截斷 (Truncation)**：
   * 當執行命令出錯時，如果錯誤堆疊（Stack Trace）非常長，工具應該只保留前 50 行與最後 50 行（這往往包含關鍵的 Crash 原因），中間的重複資訊全部予以截斷。

---

## 第六單元：用量監控與團隊成本治理架構

當團隊有多名工程師共同使用 AI 時，只靠個人自律是無法控制帳單的。我們必須在企業內部架設 **AI 中介代理服務與監控治理平台**。

### 1. 實戰 LiteLLM 架設與 API 轉接
**LiteLLM** 是一個開源的 API Gateway，它可以將各大模型廠商（OpenAI, Anthropic, Azure OpenAI, Ollama 等）的 API 封裝為統一的 OpenAI 格式，並提供完整的治理功能：
* **統一金鑰管理**：公司只需在 LiteLLM 設定一組各平台的 Master Key，再分發「虛擬 Key (Virtual Keys)」給工程師。
* **預算限額 (Spend Limits)**：為每個虛擬 Key 設定每日/每月的最大預算（例如：每位工程師每月最多使用 $50 美元額度），一旦超額即自動阻斷，防止帳單失控。
* **模型分流與備援**：當 Anthropic API 發生故障或擁堵時，自動降級/重試至 Azure Claude API，確保開發工作不中斷。

### 2. 監控指標與可觀測性 (Observability)
透過 OpenTelemetry 與 LiteLLM 的日誌對接，我們可以建立完整的用量看板：
```mermaid
graph LR
    User[開發者] -->|虛擬 Key| Proxy[LiteLLM Proxy]
    Proxy -->|API 請求| LLM[前沿模型 API]
    Proxy -->|即時日誌| DB[ClickHouse / Postgres]
    DB -->|視覺化| Grafana[Grafana 儀表板]
```
* **團隊核心追蹤指標**：
  * **誰在用？(User Tracking)**：統計每位同仁的累積消費。
  * **哪裡最燒錢？(Cost per Model/Tool)**：分析不同模型（如 Claude vs GPT）以及 MCP 工具執行的 Token 消耗佔比。
  * **快取命中率 (Cache Hit Rate)**：監控 Prompt Caching 的比例，評估工程師是否正確使用快取（若快取命中率低於 40%，代表 Prompt 結構需要優化）。

### 3. 如何建立組織級的治理制度？
1. **數據合規 (ZDR)**：在 LiteLLM 配置中強制開啟 `Zero Data Retention` 支持，並篩選屏蔽未與企業簽署隱私合約的模型平台。
2. **開發指引政策化**：將「大檔案禁止整篇載入」、「限制測試日誌貼入」等規範寫入專案的 `AGENTS.md` 作為 Agent 的執行防守邊界，讓 AI 工具在呼叫時自動遵循規範。
3. **定期成本審計**：透過 Grafana 每週自動發送超額預警與用量異常報表，即時找出「Token 異常大戶」進行工作流輔導。

---

## 預期效益

完成本課程學習後，您將帶走以下能力：
* **看懂帳單**：清楚了解每一次 Agent 交互背後的真實 Token 成本。
* **立竿見影的省錢效果**：藉由 Prompt Cache、任務拆分與工具限流，**個人或團隊的 Token 帳單可立即降低 50% 以上**。
* **防污染工作流**：學會使用分支探索與對話壓縮，維持 Agent 邏輯的敏銳度且不燒額度。
* **企業級防護罩**：透過 LiteLLM 建立可控、可監控、零隱私洩漏的團隊開發環境。

---

## 講師簡歷

**多奇數位創意有限公司 技術總監 Will 保哥**
* **Google Developer Expert (ML/Gen AI)** 谷歌開發專家
* **Microsoft Regional Director (RD)** 微軟技術社群區域總監
* **Microsoft MVP** 微軟最有價值專家 (連續 18 年當選)
* 熟悉 AI 開發工作流、DevOps、ASP.NET Core、Angular、Kubernetes 等技術。致力於將生成式 AI 規模化且安全地導入企業開發流程。





